repos:
  - repo: local
    hooks:
      - id: clang-format
        name: clang-format
        entry: clang-format -i
        language: system
        files: ^src/.*\.(c|cc|cpp|cxx|h|hh|hpp|hxx|tpp|txx|inc|fs|vs|gs|frag|vert|geom|glsl)$
        exclude: ^(external|build[^/]*|src/ui/imgui)/
